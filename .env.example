# Add any environment variables here if needed in the future
# For example, if you add analytics or other services:

# PDF.js Configuration
# PDFjs worker is currently loaded from CDN
# You can override if needed:
# VITE_PDF_WORKER_URL=https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js
